import styled from "styled-components";

const StyledWrapper = styled.div`
    display: flex;
    padding: 5px;
`
const StyledDiv = styled.div`
    position: absolute;
    right: 35px;
    display: flex;
    align-items: center;
    top: 72px;
    background-color: rgb(240 231 234);
`
export { StyledWrapper, StyledDiv }