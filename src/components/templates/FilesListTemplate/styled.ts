import styled from "styled-components";

const FileTemplateWraper = styled.div`
  max-width: 1320px;
  width: 100%;
  margin: 0 auto;
  box-sizing: border-box;
  padding: 20px 50px;
  background-color: #FCFBFB;
  box-shadow:  inset 0 0 10px #d9d9d9;
  height: calc(100vh - 60px);
`

export { FileTemplateWraper }